{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.16.4"
  },
  "workspaces": [
    "packages/*"
  ],
  "license": "Apache-2.0",
  "scripts": {
    "build": "lerna run prebuild && lerna run build",
    "testbuild": "lerna run testbuild",
    "watch": "lerna run watch",
    "clean": "lerna clean && lerna run clean",
    "test": "lerna run test --parallel"
  }
}